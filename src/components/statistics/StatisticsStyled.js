import styled from 'styled-components';
const Wrapper = styled.div`
  .stat {
    font-weight: 500;
  }
  .stat:not(:last-child) {
    margin-bottom: 10px;
  }
  .statNumber {
    font-weight: 700;
  }
`;
export default Wrapper;
